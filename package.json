{
  "name": "cot-creacion-investigadores-bot",
  "version": "1.0.0",
  "description": "ChatBot para Telegram para crear investigadores para La llamada de Cthulhu 7ed",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -Rf ./dist && babel ./ --out-dir ./dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log,./package-lock.json,./test --copy-files",
    "start": "npm run build && node dist/src/index.js"
  },
  "keywords": [
    "cot",
    "telegram",
    "llamada",
    "cthulhu"
  ],
  "author": "eduard cot ros",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "nedb-promises": "^4.0.0",
    "telegraf": "^3.32.0"
  }
}
